---
title: "BSP Image Build with firmware"
---

import HostRequirements from "../../Yocto-Guide/snippets/host-requirements.mdx"
import InstallQscCli from "../flash/snippets/install-qsc-cli.mdx"
import HostSetup from "../../Yocto-Guide/snippets/host-setup.mdx"

import Firmware6490 from "./snippets/firmware-qcs6490.mdx"
import Firmware9075 from "./snippets/firmware-iq-9075.mdx"
import Firmware8275 from "./snippets/firmware-iq-8275.mdx"
import Firmware615 from "./snippets/firmware-iq-615.mdx"

import FirmwareHostSetup from "./snippets/firmware-host-setup.mdx"
import SyncFirmware from "./sync-firmware.mdx"

import SyncInstructions from "../../Yocto-Guide/snippets/sync-meta-qcom-extras-release.mdx"
import BuildInstructions from "../../Yocto-Guide/snippets/build-meta-qcom-extras.mdx"
import ValidateInstructions from "../../Yocto-Guide/snippets/validate-build-artifacts.mdx"

import { firmware_chipsets, release_tags } from "./release-information.jsx"

## Introduction

This section provides a high-level overview of how to build firmware
components from source and integrate them into the Qualcomm BSP image.
It is intended for registered users with the appropriate licenses, as
access to firmware sources is restricted.

At a high level, this document explains how to
- Setup the host system and install the required packages - [Host Setup](#host-setup)
- Download the firmware sources from Chipcode - [Sync Firmware Sources](#sync-firmware-sources)
- Compile the firmware sources - [Build Firmware](#build-firmware)
- Include firmware sources in BSP image build - [Build BSP Image](#build-bsp-image)

## Supported Chipsets
The following chipsets are supported by this document. If your chipset is not
listed, the firmware build process described here does not apply to your device.

<table>
  <thead>
    <tr>
      <th style={{textAlign:"left"}}>Chipset</th>
      <th style={{textAlign:"left"}}>Description</th>
      <th style={{textAlign:"left"}}>Supported</th>
    </tr>
  </thead>
  <tbody>
    {firmware_chipsets.map(({ label, firmware_build_supported, description }) => (
      <tr key={label}>
        <td>{label}</td>
        <td>{description}</td>
        <td>{firmware_build_supported ? "✓" : "✗"}</td>
      </tr>
    ))}
  </tbody>
</table>

## Host System Requirements
<HostRequirements />

## Install QSC-CLI
<InstallQscCli />

## Host Setup
<HostSetup />
<FirmwareHostSetup />

## Release Tags
The following table describes the Qualcomm Yocto layers and release tags

<table>
  <thead>
    <tr>
      <th style={{textAlign:"left"}}>Release Tag</th>
      <th style={{textAlign:"left"}}>Identifier</th>
    </tr>
  </thead>
  <tbody>
    {release_tags.map(({ label, identifier}) => (
      <tr key={label}>
        <td>{label}</td>
        <td>{identifier}</td>
      </tr>
    ))}
  </tbody>
</table>

## Sync Firmware Sources
<SyncFirmware />

## Build Firmware
<Tabs>
  <Tab title="QCM6490/QCS5430">
    <Firmware6490 />
  </Tab>
  <Tab title="IQ-9075">
    <Firmware9075 />
  </Tab>
  <Tab title="IQ-8275">
    <Firmware8275 />
  </Tab>
  <Tab title="IQ-615">
    <Firmware615 />
  </Tab>
</Tabs>

## Build BSP Image
<SyncInstructions />
<BuildInstructions />
<ValidateInstructions />

## Related Topics
- [How to change default installation path for Hexagon?](./how-to-change-hexagon-install-path)
