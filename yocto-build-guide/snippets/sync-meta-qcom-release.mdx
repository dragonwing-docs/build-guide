- Download Qualcomm's Yocto layer `meta-qcom`. For the latest
   `<meta-qcom-release-tag>`, see the `Release Tags` section.

    ```bash
    git clone https://github.com/qualcomm-linux/meta-qcom -b <meta-qcom-release-tag>
    ```
